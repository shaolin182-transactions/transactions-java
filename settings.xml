<settings>
    <servers>
        <server>
            <id>github</id> <!-- Doit correspondre à l'id dans le pom.xml -->
            <username>shaolin182</username>
            <password>${env.GITHUB_TOKEN}</password> <!-- Utilise une variable d'environnement -->
        </server>
    </servers>
</settings>